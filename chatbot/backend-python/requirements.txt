# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# CORS
# (included with fastapi)

# LLM Providers
groq>=0.10.0
openai>=1.30.0

# Embeddings (local, no API key needed)
sentence-transformers>=3.0.0

# Environment variables
python-dotenv>=1.0.0

# HTTP client (Gemini provider)
httpx>=0.27.0

# PDF parsing (resume upload)
PyMuPDF>=1.24.0

# File upload parsing (FastAPI Form data)
python-multipart>=0.0.12

# S3-compatible storage (Cloudflare R2 for resume PDFs)
boto3>=1.34.0

# HTTP client (Gemini provider)
httpx>=0.27.0
